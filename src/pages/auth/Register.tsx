import { Bus } from 'lucide-react';
import { FormEvent, useState } from 'react';
import { Link, useNavigate } from 'react-router-dom';
export default function Register(){ const nav=useNavigate(); const [done,setDone]=useState(false); const submit=(e:FormEvent)=>{e.preventDefault();setDone(true);setTimeout(()=>nav('/login'),600)}; return <div className="auth-page"><section className="auth-showcase"><div className="auth-showcase-content"><div className="brand"><span className="brand-mark"><Bus/></span><span>Uni<span>Move</span></span></div><h1>Join your campus shuttle network.</h1><p>Create a student account to reserve seats, manage your trips and receive transport updates.</p></div></section><section className="auth-panel"><form className="auth-box" onSubmit={submit}><h2>Create student account</h2><p>Use your university details to get started.</p><div className="form-grid"><div className="form-group full"><label>Full name</label><input className="input" required/></div><div className="form-group"><label>Student ID</label><input className="input" required/></div><div className="form-group"><label>Department</label><input className="input" required/></div><div className="form-group full"><label>University email</label><input className="input" type="email" required/></div><div className="form-group"><label>Phone</label><input className="input" required/></div><div className="form-group"><label>Password</label><input className="input" type="password" required/></div></div><button className="btn primary">{done?'Account created':'Create account'}</button><p style={{textAlign:'center',marginTop:18}}>Already registered? <Link style={{color:'var(--primary)',fontWeight:700}} to="/login">Sign in</Link></p></form></section></div> }
