import { Filter, Search, XCircle } from 'lucide-react';
import { useState } from 'react';
import Badge from '../../components/common/Badge'; import PageHeader from '../../components/common/PageHeader'; import { reservations as seed } from '../../data/mock';
export default function Reservations(){const [items,setItems]=useState(seed);const [q,setQ]=useState('');const shown=items.filter(r=>(r.reservationNo+r.studentName+r.route).toLowerCase().includes(q.toLowerCase()));return <><PageHeader title="Reservations" subtitle="View bookings, monitor boarding status and manage cancellations."/><section className="card"><div className="table-toolbar"><div className="search-box"><Search size={15}/><input value={q} onChange={e=>setQ(e.target.value)} placeholder="Search reservation..."/></div><button className="btn secondary"><Filter size={15}/> Filter</button></div><div className="table-wrap"><table><thead><tr><th>Reservation</th><th>Student</th><th>Route</th><th>Trip</th><th>Bus / Seat</th><th>Status</th><th>Action</th></tr></thead><tbody>{shown.map(r=><tr key={r.id}><td><b>{r.reservationNo}</b></td><td>{r.studentName}<br/><span className="muted">{r.studentId}</span></td><td>{r.route}</td><td>{r.date}<br/><span className="muted">{r.time}</span></td><td>{r.bus} / {r.seat}</td><td><Badge value={r.status}/></td><td>{r.status==='reserved'?<button className="btn secondary sm" onClick={()=>setItems(v=>v.map(x=>x.id===r.id?{...x,status:'cancelled' as const}:x))}><XCircle size={14}/> Cancel</button>:'—'}</td></tr>)}</tbody></table></div></section></>}
