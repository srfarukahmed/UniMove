import { Edit3, Plus, Trash2, Wrench } from 'lucide-react';
import { FormEvent, useState } from 'react'; import Badge from '../../components/common/Badge'; import Modal from '../../components/common/Modal'; import PageHeader from '../../components/common/PageHeader';
type Row={id:number;bus:string;date:string;cost:number;mileage:number;provider:string;next:string;status:string};const seed:Row[]=[{id:1,bus:'UM-02',date:'2026-09-15',cost:18500,mileage:58210,provider:'Metro Auto Works',next:'2026-12-15',status:'maintenance'},{id:2,bus:'UM-01',date:'2026-08-25',cost:7600,mileage:49880,provider:'Campus Workshop',next:'2026-11-25',status:'completed'}];
export default function Maintenance(){const [items,setItems]=useState(seed);const [open,setOpen]=useState(false);const [edit,setEdit]=useState<Row|null>(null);const save=(e:FormEvent<HTMLFormElement>)=>{e.preventDefault();const f=new FormData(e.currentTarget);const row:Row={id:edit?.id||Date.now(),bus:String(f.get('bus')),date:String(f.get('date')),cost:Number(f.get('cost')),mileage:Number(f.get('mileage')),provider:String(f.get('provider')),next:String(f.get('next')),status:String(f.get('status'))};setItems(v=>edit?v.map(x=>x.id===edit.id?row:x):[row,...v]);setOpen(false)};return <><PageHeader title="Maintenance" subtitle="Track service history, mileage, cost and next service date." action={<button className="btn primary" onClick={()=>{setEdit(null);setOpen(true)}}><Plus size={15}/> Add record</button>}/><section className="card"><div className="table-wrap"><table><thead><tr><th>Bus</th><th>Service date</th><th>Mileage</th><th>Cost</th><th>Provider</th><th>Next service</th><th>Status</th><th>Actions</th></tr></thead><tbody>{items.map(r=><tr key={r.id}><td><div className="list-main"><Wrench size={14} color="var(--primary)"/><b>{r.bus}</b></div></td><td>{r.date}</td><td>{r.mileage.toLocaleString()} km</td><td>৳{r.cost.toLocaleString()}</td><td>{r.provider}</td><td>{r.next}</td><td><Badge value={r.status}/></td><td><div className="row-actions"><button className="icon-btn" onClick={()=>{setEdit(r);setOpen(true)}}><Edit3 size={15}/></button><button className="icon-btn" onClick={()=>setItems(v=>v.filter(x=>x.id!==r.id))}><Trash2 size={15}/></button></div></td></tr>)}</tbody></table></div></section><Modal open={open} title={edit?'Edit maintenance record':'Add maintenance record'} onClose={()=>setOpen(false)}><form onSubmit={save}><div className="form-grid"><div className="form-group"><label>Bus</label><select name="bus" className="select" defaultValue={edit?.bus}><option>UM-01</option><option>UM-02</option><option>UM-03</option></select></div><div className="form-group"><label>Service date</label><input name="date" type="date" className="input" defaultValue={edit?.date||'2026-09-17'}/></div><div className="form-group"><label>Cost (BDT)</label><input name="cost" type="number" className="input" defaultValue={edit?.cost||0}/></div><div className="form-group"><label>Mileage (km)</label><input name="mileage" type="number" className="input" defaultValue={edit?.mileage||0}/></div><div className="form-group"><label>Service provider</label><input name="provider" className="input" defaultValue={edit?.provider}/></div><div className="form-group"><label>Next service</label><input name="next" type="date" className="input" defaultValue={edit?.next}/></div><div className="form-group full"><label>Status</label><select name="status" className="select" defaultValue={edit?.status||'maintenance'}><option>maintenance</option><option>completed</option></select></div></div><div className="form-actions"><button className="btn primary">Save record</button></div></form></Modal></>}
