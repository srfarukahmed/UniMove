import { AlertTriangle, CheckCircle2, Search } from 'lucide-react';
import { useState } from 'react'; import Badge from '../../components/common/Badge'; import PageHeader from '../../components/common/PageHeader';
const seed=[{id:'INC-0917-12',type:'Overcrowding',bus:'UM-03',route:'Campus ↔ Dhanmondi',reporter:'Sadia Karim',priority:'high',status:'open',time:'08:18'},{id:'INC-0916-09',type:'Lost item',bus:'UM-01',route:'Campus ↔ Uttara',reporter:'Ayesha Rahman',priority:'normal',status:'resolved',time:'17:44'},{id:'INC-0916-03',type:'Bus problem',bus:'UM-02',route:'Campus ↔ Mirpur',reporter:'Nayeem Hasan',priority:'high',status:'in-review',time:'07:52'}];
export default function Incidents(){const [items,setItems]=useState(seed);const [q,setQ]=useState('');const shown=items.filter(x=>(x.id+x.type+x.bus+x.reporter).toLowerCase().includes(q.toLowerCase()));return <><PageHeader title="Incidents" subtitle="Review student reports, transport issues and resolution status."/><section className="card"><div className="table-toolbar"><div className="search-box"><Search size={15}/><input placeholder="Search incidents..." value={q} onChange={e=>setQ(e.target.value)}/></div></div><div className="table-wrap"><table><thead><tr><th>Incident</th><th>Type</th><th>Bus / Route</th><th>Reporter</th><th>Priority</th><th>Status</th><th>Action</th></tr></thead><tbody>{shown.map(i=><tr key={i.id}><td><b>{i.id}</b><br/><span className="muted">{i.time}</span></td><td><div className="list-main"><AlertTriangle size={14} color="var(--warning)"/>{i.type}</div></td><td>{i.bus}<br/><span className="muted">{i.route}</span></td><td>{i.reporter}</td><td><Badge value={i.priority}/></td><td><Badge value={i.status}/></td><td><button className="btn primary sm" onClick={()=>setItems(v=>v.map(x=>x.id===i.id?{...x,status:'resolved'}:x))}><CheckCircle2 size={14}/> Resolve</button></td></tr>)}</tbody></table></div></section></>}
