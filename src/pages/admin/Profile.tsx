import { Save, ShieldCheck } from 'lucide-react'; import { FormEvent, useState } from 'react'; import PageHeader from '../../components/common/PageHeader'; import { useAuth } from '../../hooks/useAuth';
export default function Profile(){const {user,updateProfile}=useAuth();const [name,setName]=useState(user?.name||'');const [phone,setPhone]=useState(user?.phone||'');const [saved,setSaved]=useState(false);const submit=(e:FormEvent)=>{e.preventDefault();updateProfile({name,phone});setSaved(true);setTimeout(()=>setSaved(false),1400)};return <><PageHeader title="Admin Profile" subtitle="Manage administrator information and account security."/><div className="grid cols-3"><section className="card"><div style={{textAlign:'center'}}><div className="list-icon" style={{width:86,height:86,margin:'0 auto 12px',borderRadius:24}}><ShieldCheck size={34}/></div><h3>{user?.name}</h3><p className="muted" style={{fontSize:11}}>System Administrator</p><span className="badge active">Active</span></div></section><section className="card span-2"><div className="card-head"><div><h3>Account information</h3><p>Update administrator contact details</p></div>{saved&&<span className="badge active">Saved</span>}</div><form onSubmit={submit}><div className="form-grid"><div className="form-group"><label>Name</label><input className="input" value={name} onChange={e=>setName(e.target.value)}/></div><div className="form-group"><label>Phone</label><input className="input" value={phone} onChange={e=>setPhone(e.target.value)}/></div><div className="form-group full"><label>Email</label><input className="input" value={user?.email||''} readOnly/></div></div><div className="form-actions"><button className="btn primary"><Save size={15}/> Save profile</button></div></form></section></div></>}
