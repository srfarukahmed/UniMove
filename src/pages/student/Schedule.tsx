import { CalendarDays, Filter } from 'lucide-react';
import { useState } from 'react';
import { Link } from 'react-router-dom';
import Badge from '../../components/common/Badge';
import PageHeader from '../../components/common/PageHeader';
import { trips } from '../../data/mock';
export default function Schedule(){ const [route,setRoute]=useState('all'); const visible=route==='all'?trips:trips.filter(t=>String(t.routeId)===route); return <><PageHeader title="Shuttle Schedule" subtitle="Daily departures, assigned buses and seat availability." action={<div className="toolbar-group"><select className="select" value={route} onChange={e=>setRoute(e.target.value)}><option value="all">All routes</option><option value="1">Campus ↔ Uttara</option><option value="2">Campus ↔ Dhanmondi</option><option value="3">Campus ↔ Mirpur</option></select></div>}/><section className="card"><div className="table-toolbar"><div className="list-main"><div className="list-icon"><CalendarDays size={16}/></div><div className="list-copy"><b>Thursday, 17 September 2026</b><span>{visible.length} scheduled trips</span></div></div><button className="btn secondary sm"><Filter size={14}/> Filter</button></div><div className="table-wrap"><table><thead><tr><th>Route</th><th>Bus</th><th>Departure</th><th>Arrival</th><th>Seats</th><th>Status</th><th>Action</th></tr></thead><tbody>{visible.map(t=><tr key={t.id}><td><b>{t.routeName}</b></td><td>{t.busNumber}</td><td>{t.departure}</td><td>{t.arrival}</td><td>{t.seatsAvailable}/{t.capacity}</td><td><Badge value={t.status}/></td><td><Link className="btn primary sm" to={`/student/reserve/${t.id}`}>Reserve</Link></td></tr>)}</tbody></table></div></section></> }
